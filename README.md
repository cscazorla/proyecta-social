# Proyecta Social

Static website for **Proyecta Social** — consultancy firm specializing in non-governmental organizations.

## Stack

Plain HTML + CSS. No build step required.

## Local development

Open `index.html` in a browser, or use any static file server:

```bash
npx serve .
```
